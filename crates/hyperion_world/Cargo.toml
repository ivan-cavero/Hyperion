[package]
name = "hyperion_world"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Hyperion world: chunks, 1:1 worldgen, world formats"

[dependencies]
hyperion_protocol = { path = "../hyperion_protocol" }
flate2 = { workspace = true }
thiserror = { workspace = true }
# Worldgen datapack JSON (noise params, density functions).
serde_json = { workspace = true }
# Read official server-inner.jar for density/noise_settings (dev/tests).
zip = { workspace = true }
# RandomSupport.seedFromHashOf (MD5) — same as JE RandomState noise wiring.
md5 = { workspace = true }

[lints]
workspace = true
