[package]
name = "hyperion_world"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Hyperion world: chunks, 1:1 worldgen, world formats"

[dependencies]
# Phase 2: will add whitelist deps (flate2, serde…) + optional FFI to cubiomes (MIT).