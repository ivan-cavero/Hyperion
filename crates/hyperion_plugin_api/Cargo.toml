[package]
name = "hyperion_plugin_api"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Hyperion plugin API: WASM/WIT ABI, events, commands, scripting"

[dependencies]
# Phase 4: will add whitelist deps (wasmtime, mlua).