[package]
name = "hyperion_simulation"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Hyperion simulation: per-region ticking, ECS, entities"

[dependencies]
# Phase 3: will add whitelist deps (rayon, crossbeam, parking_lot, dashmap)
# + open decision: bevy_ecs vs custom ECS.

[lints]
workspace = true